export interface IPromocion {
    nroRestaurante: string;
    nroContenido:number;
    nroSucursal: number;
    contenidoPromocional: string;
    imagenPromocional: string;

    //proposito?: string;
}